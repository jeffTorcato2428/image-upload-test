import React from 'react'
import styled from '@emotion/styled';

const ImageDropBefore = () => {

    const Wrapper = styled('div')({
        display: 'flex',
        position: 'relative',
        flexGrow: 'inherit',
        borderStyle: 'dashed'
      });
    return (
        <Wrapper>

        </Wrapper>
    )
}

export default ImageDropBefore