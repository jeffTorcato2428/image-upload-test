import React from 'react'
import styled from '@emotion/styled';

const ImageDropAfter = () => {
    const Wrapper = styled('div')({
        display: 'flex',
        position: 'relative',
        flexGrow: 'inherit',
        borderStyle: 'dashed'
      });
    return (
        <Wrapper>

        </Wrapper>
    )
}

export default ImageDropAfter